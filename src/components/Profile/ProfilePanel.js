import React from "react";

const ProfilePanel = ({ props }) => {
  return (
    <div className="flex flex-row bg-amber-300">
      <div className="">sdaadada</div>
    </div>
  );
};

export default ProfilePanel;
